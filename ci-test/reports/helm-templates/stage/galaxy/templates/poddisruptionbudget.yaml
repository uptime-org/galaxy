---
# Source: galaxy/templates/poddisruptionbudget.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: galaxy-stage
  namespace: stage
  labels:
    app.kubernetes.io/name: galaxy
    app.kubernetes.io/instance: galaxy-stage
    app.kubernetes.io/environment: stage
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: galaxy
      app.kubernetes.io/instance: galaxy-stage
